self.addEventListener("push",event=>{const data=event.data?.json()??{};event.waitUntil(self.registration.showNotification(data.title??"Hevy Coach",{body:data.body??"Você tem um novo insight.",icon:"/icon.svg",badge:"/icon.svg",tag:data.tag??"hevy-coach",data:{href:data.href??"/"},vibrate:[120,60,120]}))});
self.addEventListener("notificationclick",event=>{event.notification.close();event.waitUntil(clients.matchAll({type:"window",includeUncontrolled:true}).then(list=>{const href=new URL(event.notification.data?.href??"/",self.location.origin).href;for(const client of list){if(client.url.startsWith(self.location.origin)&&"focus" in client){client.navigate(href);return client.focus()}}return clients.openWindow(href)}))});
